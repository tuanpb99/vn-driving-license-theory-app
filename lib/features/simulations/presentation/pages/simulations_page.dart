import 'package:flutter/material.dart';

class SimulationsPage extends StatelessWidget {
  const SimulationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('120 tình huống mô phỏng')),
      body: const Center(
        child: Text('Sẵn sàng mở rộng module mô phỏng trong MVP tiếp theo.'),
      ),
    );
  }
}
